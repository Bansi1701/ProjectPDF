# --- OS ---
.DS_Store
Thumbs.db

# --- Editors ---
.vscode/
.idea/
*.swp

# --- Node / Astro ---
node_modules/
dist/
.astro/
*.tsbuildinfo
npm-debug.log*
yarn-error.log*

# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- Secrets ---
.env
.env.*
!.env.example

# --- Docker ---
# compose overrides are per-developer
docker-compose.override.yml

# --- Build / test artifacts ---
/tmp/
*.log

# --- Project ---
# Never commit test PDFs that came from a real user.
/corpus/

# The business plan is not public. Keep strategy documents out of the repo.
Docs/*.pdf

# --- Build artifacts copied from node_modules (see frontend/scripts) ---

# --- Agent worktrees ---
.claude/worktrees/
# Vendored out of node_modules at build time by the pdfjs-assets integration.

# Vendored from node_modules by `npm run assets`; regenerated on every build.

# Vendored from node_modules by `npm run assets`; rebuilt on every build.

# Rebuilt from node_modules on every build by `npm run assets` and the
# pdfjs-assets integration. Committing them would put ~30 MB of derived
# binaries in the history for no benefit.
frontend/public/tesseract/
frontend/public/pdfjs/
frontend/public/wasm/
frontend/.tmp-test/

# NOT ignored: frontend/public/tessdata/ — the OCR language data is fetched
# once by hand, not produced by the build, so a clean checkout needs it.
