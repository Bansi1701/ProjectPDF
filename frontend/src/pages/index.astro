---
import BaseLayout from '../layouts/BaseLayout.astro';
import Masthead from '../components/Masthead.astro';
import Lens from '../components/Lens.astro';
import { CATEGORIES, SERVER_ONLY_REASONS, SITE, toolsByCategory } from '../config/site';

const steps = [
  { n: '1', title: 'You pick a file', body: 'The browser hands the tool a copy in memory. No request is made.' },
  { n: '2', title: 'Your CPU does the work', body: 'A Web Worker runs the operation off the main thread, so the page stays responsive.' },
  { n: '3', title: 'You save the result', body: 'Close the tab and every trace of it is gone. There was never a second copy.' },
];

/* What a completed job is allowed to log. Mirrors the repo's logging rule:
   job id, tool, duration, byte counts — never filenames or content. */
const crossings: [string, string][] = [
  ['job', 'a3f9c1'],
  ['tool', 'merge'],
  ['duration', '412ms'],
  ['bytes out', '0'],
];

const promises = [
  { figure: '0', label: 'bytes uploaded' },
  { figure: '0', label: 'files stored' },
  { figure: '0', label: 'accounts required' },
  { figure: '0', label: 'reasons to trust us' },
];

const baseUrl = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : `${import.meta.env.BASE_URL}/`;
const brandMark = `${baseUrl}brand/pdfcraft-fold-mark.png`;
---

<BaseLayout title={`${SITE.name} — ${SITE.tagline}`} description={SITE.description}>
  <Masthead />

  <main id="main">
    <!-- ── Hero: the desk ─────────────────────────────────────────────── -->
    <section class="hero wrap">
      <div class="brand-cascade" aria-hidden="true">
        <span class="brand-watermark brand-watermark--one"><img src={brandMark} alt="" /></span>
        <span class="brand-watermark brand-watermark--two"><img src={brandMark} alt="" /></span>
        <span class="brand-watermark brand-watermark--three"><img src={brandMark} alt="" /></span>
      </div>
      <div class="hero__copy">
        <p class="eyebrow"><span class="dot"></span>Private document workspace</p>

        <h1>Every PDF task,<br />shaped around your document.</h1>

        <p class="lede">
          A focused place to organise, convert, edit and protect PDFs. The work stays in your
          browser, and every result is ready to inspect before you save it.
        </p>

        <ul class="hero__proof" aria-label="PDFCraft benefits">
          <li><span>01</span> Browser-side processing</li>
          <li><span>02</span> Visible page controls</li>
          <li><span>03</span> Ready-to-save results</li>
        </ul>

        <div class="hero__actions">
          <a class="btn btn--primary" href="#tools">Explore tools</a>
          <a class="btn btn--quiet" href="#how">How privacy works</a>
        </div>

        <p class="hero__meta">Free · No account · Your files stay on this device</p>
      </div>

      <!-- The metaphor: a sheet of paper with a glass lens laid across it -->
      <figure class="hero__desk" aria-hidden="true">
        <div class="sheet page">
          <div class="page__topline"><span>PDFCRAFT / DOCUMENT STUDIO</span><i></i></div>
          <div class="page__rule page__rule--title"></div>
          <div class="page__rule" style="width: 92%"></div>
          <div class="page__rule" style="width: 86%"></div>
          <div class="page__rule" style="width: 94%"></div>
          <div class="page__rule" style="width: 61%"></div>
          <div class="page__gap"></div>
          <div class="page__rule" style="width: 88%"></div>
          <div class="page__rule" style="width: 95%"></div>
          <div class="page__rule" style="width: 72%"></div>
          <div class="page__footer"><span></span><span></span><span></span></div>
        </div>

        <Lens variant="loupe" class="loupe">
          <p class="loupe__label">Document stage</p>
          <p class="loupe__figure">Ready <span>to work</span></p>
          <p class="loupe__status"><i class="led"></i>processing: on device</p>
        </Lens>

        <ul class="hero__tool-orbit">
          <li class="orbit-card orbit-card--merge"><b>↗</b><span>Merge</span></li>
          <li class="orbit-card orbit-card--split"><b>✦</b><span>Split</span></li>
          <li class="orbit-card orbit-card--secure"><b>⌁</b><span>Protect</span></li>
        </ul>
      </figure>
    </section>

    <!-- ── The receipt ────────────────────────────────────────────────── -->
    <section class="receipt">
      <div class="wrap">
        <ul class="receipt__row">
          {
            promises.map((item) => (
              <li>
                <span class="receipt__figure">{item.figure}</span>
                <span class="receipt__label">{item.label}</span>
              </li>
            ))
          }
        </ul>
        <p class="receipt__note">
          Not a badge we award ourselves. When a tool ships, open your browser's Network
          tab and use it — you will watch nothing happen.
        </p>

        <div class="boundary"><span>device boundary</span></div>

        <ul class="telemetry">
          {
            crossings.map(([key, value]) => (
              <li>
                <span>{key}</span>
                <b>{value}</b>
              </li>
            ))
          }
        </ul>
        <p class="telemetry__note">
          The only things that will ever cross it. No filename, no page count, no text —
          those never leave the tab in the first place.
        </p>
      </div>
    </section>

    <!-- ── Tools ──────────────────────────────────────────────────────── -->
    <section class="tools wrap reveal" id="tools">
      <header class="section-head">
        <h2>Choose your next step</h2>
        <p class="section-head__deck">
          Start with a clear outcome. Every tool labelled <em>in your browser</em> handles the
          document on your machine.
        </p>
        <label class="tool-search">
          <span class="sr-only">Search tools</span>
          <input type="search" data-tool-search placeholder="Search merge, watermark, protect…" autocomplete="off" />
        </label>
        <p class="tool-search__empty" data-tool-search-empty hidden>No tool matches that search yet.</p>
      </header>

      {
        CATEGORIES.map((category) => (
          <div class="cluster" id={category.toLowerCase()}>
            <h3 class="cluster__name">{category}</h3>
            <ul class="grid">
              {toolsByCategory(category).map((tool) => (
                <li class="sheet card" data-tool-card data-search={`${tool.name} ${tool.blurb} ${category}`.toLowerCase()}>
                  {tool.status === 'live' && <a class="card__link" href={`${baseUrl}${tool.slug}/`} aria-label={`Open ${tool.name}`}></a>}
                  <p class="card__category">{category}</p>
                  <h4 class="card__name">{tool.name}</h4>
                  <p class="card__blurb">{tool.blurb}</p>
                  <p class:list={['card__where', tool.runsWhere === 'server' && 'is-server']}>
                    {tool.runsWhere === 'local' ? 'in your browser' : 'needs our server'}
                    {tool.status === 'building' && <span class="card__soon">· building now</span>}
                  </p>
                </li>
              ))}
            </ul>
          </div>
        ))
      }
    </section>

    <!-- ── How it works ───────────────────────────────────────────────── -->
    <section class="how reveal" id="how">
      <div class="wrap">
        <header class="section-head">
          <h2>How it works</h2>
          <p class="section-head__deck">
            The mechanism is the trust. If you can follow it, you do not have to take our
            word for anything — and you can see exactly which step is missing.
          </p>
        </header>

        <ol class="steps">
          {
            steps.map((step) => (
              <li class="sheet step">
                <span class="step__n">{step.n}</span>
                <h3 class="step__title">{step.title}</h3>
                <p class="step__body">{step.body}</p>
              </li>
            ))
          }

          <!-- The absence is the argument. Deliberately empty. -->
          <li class="step step--absent">
            <span class="step__n">4</span>
            <h3 class="step__title">Upload to our server</h3>
            <p class="step__body">Not implemented. There is no endpoint to call.</p>
          </li>
        </ol>

        <p class="how__never">
          <span class="how__strike">Your file → our server → back to you</span>
          <span class="how__seal">never happens</span>
        </p>
      </div>
    </section>

    <!-- ── Honest limits ──────────────────────────────────────────────── -->
    <section class="limits wrap reveal">
      <div class="limits__intro">
        <h2>What we can't do in your browser</h2>
        <p class="lede">
          Six things genuinely need a server. We would rather name them than quietly route
          your file somewhere and hope you don't check.
        </p>
        <p class="limits__closing">
          These are labelled wherever they appear. The file is deleted the moment the job
          finishes — and it is the only time a file of yours leaves your machine at all.
        </p>
      </div>

      <dl class="limits__list">
        {
          SERVER_ONLY_REASONS.map((item) => (
            <div class="limits__item">
              <dt>{item.what}</dt>
              <dd>{item.why}</dd>
            </div>
          ))
        }
      </dl>
    </section>

    <!-- ── Close ──────────────────────────────────────────────────────── -->
    <section class="close reveal">
      <div class="wrap">
        <h2 class="close__line">Pick a file. Shape the result.<br />Keep it private.</h2>
        <p class="close__note">
          Every local tool keeps the document in the browser tab. When you save, you take the
          result with you — nothing is left behind.
        </p>
      </div>
    </section>
  </main>

  <footer class="footer">
    <div class="wrap">
      <div class="footer__cols">
        {
          CATEGORIES.map((category) => (
            <div>
              <h4>{category}</h4>
              <ul>
                {toolsByCategory(category).map((tool) => (
                  <li>{tool.name}</li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>

      <div class="footer__base">
        <span class="footer__brand">{SITE.name}</span>
        <span class="footer__colophon">
          Appearance follows your system · Built so that we couldn't read your documents
          even if we wanted to.
        </span>
      </div>
    </div>
  </footer>
</BaseLayout>

<script>
  const search = document.querySelector<HTMLInputElement>('[data-tool-search]');
  const cards = [...document.querySelectorAll<HTMLElement>('[data-tool-card]')];
  const empty = document.querySelector<HTMLElement>('[data-tool-search-empty]');

  search?.addEventListener('input', () => {
    const query = search.value.trim().toLowerCase();
    let visible = 0;
    cards.forEach((card) => {
      const match = !query || card.dataset.search?.includes(query);
      card.hidden = !match;
      if (match) visible += 1;
    });
    if (empty) empty.hidden = visible !== 0;
  });
</script>

<style>
  /* ── Hero ─────────────────────────────────────────────────────────── */
  .hero {
    position: relative;
    isolation: isolate;
    display: grid;
    gap: clamp(3rem, 2rem + 5vw, 5rem);
    padding-block: clamp(4.5rem, 3rem + 6vw, 7.75rem) clamp(4rem, 3rem + 5vw, 7rem);
  }

  .hero::before {
    content: '';
    position: absolute;
    z-index: -2;
    width: min(43vw, 39rem);
    aspect-ratio: 1;
    right: -16rem;
    top: 6%;
    border-radius: 50%;
    background: radial-gradient(circle at 32% 36%, rgb(251 113 133 / 0.45), transparent 25%),
      radial-gradient(circle at 65% 57%, rgb(125 211 252 / 0.38), transparent 36%),
      radial-gradient(circle at 48% 47%, rgb(196 181 253 / 0.34), transparent 67%);
    filter: blur(6px);
  }

  .hero::after {
    content: '';
    position: absolute;
    z-index: -1;
    inset: auto 0 0;
    height: 1px;
    background: linear-gradient(90deg, transparent, var(--line), transparent);
  }

  /* The brand appears as a physical impression on the workspace, never as a
     competing hero illustration. The primary mark is exactly 3× the header
     mark (5.25rem vs. 1.75rem); the two small marks make the motif memorable. */
  .brand-cascade {
    position: absolute;
    z-index: -1;
    inset: -3.1rem 0 auto;
    height: 10rem;
    overflow: hidden;
    pointer-events: none;
  }

  .brand-watermark {
    position: absolute;
    display: block;
    padding-top: 1.6rem;
    opacity: 0.11;
    filter: drop-shadow(0 9px 9px rgb(15 23 42 / 0.18));
  }

  .brand-watermark::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    width: 1px;
    height: 1.6rem;
    background: linear-gradient(transparent, color-mix(in srgb, var(--accent) 45%, var(--line)));
  }

  .brand-watermark img { display: block; width: 100%; height: auto; }

  .brand-watermark--one {
    top: 0;
    left: 14%;
    width: 5.25rem;
    transform: rotate(-7deg);
  }

  .brand-watermark--two {
    top: 1.7rem;
    left: 53%;
    width: 3.1rem;
    opacity: 0.07;
    transform: rotate(11deg);
  }

  .brand-watermark--three {
    top: -0.75rem;
    right: 8%;
    width: 4rem;
    opacity: 0.08;
    transform: rotate(-14deg);
  }

  :global(:root[data-theme='dark']) .brand-watermark {
    opacity: 0.23;
    filter: grayscale(0.2) brightness(2.8) contrast(0.65) drop-shadow(0 10px 16px rgb(0 0 0 / 0.55));
    mix-blend-mode: screen;
  }

  :global(:root[data-theme='dark']) .brand-watermark--one { opacity: 0.3; }
  :global(:root[data-theme='dark']) .brand-watermark--two { opacity: 0.16; }
  :global(:root[data-theme='dark']) .brand-watermark--three { opacity: 0.18; }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .dot {
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--ok);
  }

  h1 {
    font-size: var(--step-5);
    margin-block: 1.25rem 1.5rem;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 2rem;
  }

  .hero__meta {
    margin-top: 1.25rem;
    font-size: var(--step--2);
    color: var(--muted);
  }

  /* the desk: a sheet with a lens lying across its lower edge */
  .hero__desk {
    position: relative;
    isolation: isolate;
    margin: 0;
    padding: 1.5rem 1.5rem 4.5rem;
    border: 1px solid color-mix(in srgb, var(--line) 88%, transparent);
    border-radius: 28px;
    background: linear-gradient(145deg, color-mix(in srgb, var(--paper-raised) 88%, transparent), color-mix(in srgb, var(--paper-sunk) 76%, transparent));
    box-shadow: 0 32px 60px -46px rgb(15 23 42 / 0.5);
  }

  /* Light falling across the desk. Without something varied behind it, the
     lens has nothing to refract and reads as a flat card. */
  .hero__desk::before {
    content: '';
    position: absolute;
    inset: -14% -10% -8%;
    z-index: -1;
    background:
      radial-gradient(58% 52% at 74% 14%, var(--desk-warm), transparent 68%),
      radial-gradient(52% 48% at 16% 84%, var(--desk-cool), transparent 70%),
      radial-gradient(44% 40% at 92% 76%, var(--desk-blush), transparent 72%);
    filter: blur(10px);
    pointer-events: none;
  }

  .hero__proof {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem 1.1rem;
    margin: 1.75rem 0 0;
    padding: 0;
    list-style: none;
    color: var(--ink-2);
    font-size: var(--step--2);
    font-weight: 600;
  }

  .hero__proof li { display: inline-flex; align-items: center; gap: 0.4rem; }
  .hero__proof span {
    display: grid;
    place-items: center;
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 50%;
    color: var(--accent);
    background: color-mix(in srgb, var(--accent) 10%, transparent);
    font-family: var(--font-mono);
    font-size: 0.56rem;
  }

  .hero__desk::after {
    content: '';
    position: absolute;
    width: 11rem;
    height: 4.5rem;
    right: 18%;
    top: -1.25rem;
    border-top: 1px solid color-mix(in srgb, var(--accent) 45%, var(--line));
    border-radius: 50%;
    transform: rotate(-18deg);
    opacity: 0.55;
    pointer-events: none;
  }

  .page {
    padding: clamp(1.5rem, 1rem + 2vw, 2.25rem);
    display: grid;
    gap: 0.75rem;
    transform: rotate(-1.1deg) translateX(-0.35rem);
    box-shadow: 0 20px 45px -32px rgb(15 23 42 / 0.45);
  }

  .page__topline {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.6rem;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.55rem;
    letter-spacing: 0.12em;
  }

  .page__topline i {
    width: 0.55rem;
    height: 0.55rem;
    border-radius: 50%;
    background: var(--accent);
    box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent) 10%, transparent);
  }

  .page__rule {
    height: 0.5rem;
    border-radius: 2px;
    background: var(--line);
    width: 100%;
  }

  .page__rule--title {
    height: 0.875rem;
    width: 54%;
    background: var(--line-strong);
    margin-bottom: 0.5rem;
  }

  .page__gap {
    height: 0.75rem;
  }

  .page__footer {
    display: flex;
    gap: 0.3rem;
    margin-top: 0.55rem;
  }

  .page__footer span {
    display: block;
    width: 1.25rem;
    height: 0.25rem;
    border-radius: 99px;
    background: var(--line);
  }

  .page__footer span:first-child { background: var(--accent); width: 2.1rem; }

  /* Rendered by <Lens>, so it needs :global() constrained to this section. */
  .hero__desk :global(.loupe) {
    position: absolute;
    right: clamp(-1.75rem, -3vw, 0.75rem);
    bottom: -0.35rem;
    width: min(16rem, 70%);
    padding: 1.125rem 1.25rem;
  }

  .loupe__label {
    font-size: var(--step--2);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--muted);
  }

  .loupe__figure {
    font-family: var(--font-mono);
    font-size: var(--step-3);
    font-weight: 500;
    color: var(--ink);
    letter-spacing: -0.03em;
    margin-block: 0.25rem;
  }

  .loupe__figure span {
    font-size: var(--step-1);
    color: var(--muted);
  }

  .loupe__status {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-size: var(--step--2);
    color: var(--ink-2);
    padding-top: 0.75rem;
    border-top: 1px solid var(--line);
  }

  .led {
    width: 0.4rem;
    height: 0.4rem;
    border-radius: 50%;
    background: var(--ok);
    box-shadow: 0 0 8px color-mix(in oklab, var(--ok) 60%, transparent);
  }

  .hero__tool-orbit {
    position: absolute;
    left: -1rem;
    bottom: 1.1rem;
    display: flex;
    flex-wrap: wrap;
    gap: 0.45rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .orbit-card {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    padding: 0.45rem 0.65rem;
    border: 1px solid color-mix(in srgb, var(--line-strong) 78%, transparent);
    border-radius: 10px;
    color: var(--ink);
    background: color-mix(in srgb, var(--paper-raised) 92%, transparent);
    box-shadow: 0 9px 20px -16px rgb(15 23 42 / 0.48);
    font-size: var(--step--2);
    font-weight: 600;
  }

  .orbit-card b {
    display: grid;
    place-items: center;
    width: 1.15rem;
    height: 1.15rem;
    border-radius: 5px;
    color: white;
    background: var(--accent);
    font-size: 0.7rem;
  }

  .orbit-card--split b { background: #7c3aed; }
  .orbit-card--secure b { background: #0f766e; }

  @media (min-width: 62em) {
    .hero {
      grid-template-columns: 1.05fr 0.95fr;
      align-items: center;
    }
  }

  /* ── Receipt ──────────────────────────────────────────────────────── */
  .receipt {
    background: var(--paper-sunk);
    border-block: 1px solid var(--line);
    padding-block: clamp(2.5rem, 2rem + 2vw, 3.5rem);
  }

  .receipt__row {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1px;
    margin: 0;
    padding: 0;
    list-style: none;
    background: var(--line);
    border: 1px solid var(--line);
  }

  .receipt__row li {
    background: var(--paper-sunk);
    padding: 1.25rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.125rem;
  }

  .receipt__figure {
    font-family: var(--font-mono);
    font-size: var(--step-3);
    color: var(--ok);
    line-height: 1;
  }

  .receipt__label {
    font-size: var(--step--1);
    color: var(--ink-2);
  }

  .receipt__note {
    margin-top: 1.5rem;
    max-width: var(--measure);
    color: var(--ink-2);
  }

  /* A drawn line, so "crosses the boundary" is a place and not a phrase. */
  .boundary {
    position: relative;
    margin: 2.75rem 0 1.25rem;
    border-top: 1px solid var(--ok);
  }

  .boundary span {
    position: absolute;
    top: -0.62rem;
    left: 0;
    padding-right: 0.75rem;
    background: var(--paper-sunk);
    font-family: var(--font-mono);
    font-size: var(--step--2);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ok);
  }

  .telemetry {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 2rem;
    margin: 0;
    padding: 0;
    list-style: none;
    font-family: var(--font-mono);
    font-size: var(--step--1);
  }

  .telemetry li {
    display: flex;
    gap: 0.5rem;
  }

  .telemetry span {
    color: var(--muted);
  }

  .telemetry b {
    font-weight: 500;
    color: var(--ink);
  }

  .telemetry__note {
    margin-top: 1rem;
    max-width: var(--measure);
    font-size: var(--step--1);
    color: var(--muted);
  }

  @media (min-width: 48em) {
    .receipt__row {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* ── Sections ─────────────────────────────────────────────────────── */
  .section-head {
    margin-bottom: clamp(2rem, 1.5rem + 2vw, 3rem);
  }

  .section-head h2 {
    font-size: var(--step-3);
  }

  .section-head__deck {
    margin-top: 0.875rem;
    max-width: var(--measure);
    color: var(--ink-2);
  }

  .section-head__deck em {
    font-style: normal;
    color: var(--ok);
  }

  @media (max-width: 30em) {
    .hero__desk { padding: 1rem 1rem 4rem; }
    .hero__tool-orbit { left: 0.25rem; bottom: 0.65rem; }
    .orbit-card { padding: 0.35rem 0.45rem; }
    .orbit-card span { display: none; }
  }

  .tool-search {
    display: block;
    max-width: 36rem;
    margin-top: 1.25rem;
  }

  .tool-search input {
    width: 100%;
    min-height: 3rem;
    padding: 0.75rem 1rem;
    color: var(--ink);
    background: var(--paper-raised);
    border: 1px solid var(--line-strong);
    border-radius: 12px;
    box-shadow: 0 1px 2px rgb(15 23 42 / 0.03);
    font: inherit;
  }

  .tool-search input::placeholder { color: var(--muted); }

  .tool-search__empty {
    margin-top: 0.75rem;
    color: var(--muted);
    font-size: var(--step--1);
  }

  .tools,
  .limits {
    padding-block: clamp(3.5rem, 2.5rem + 4vw, 6rem);
  }

  .how,
  .close {
    background: var(--paper-sunk);
    border-block: 1px solid var(--line);
    padding-block: clamp(3.5rem, 2.5rem + 4vw, 6rem);
  }

  /* ── Tool grid ────────────────────────────────────────────────────── */
  .cluster {
    margin-bottom: 2.5rem;
    scroll-margin-top: 6rem;
  }

  .cluster__name {
    font-size: var(--step--2);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    font-weight: 500;
    padding-bottom: 0.75rem;
    border-bottom: 1px solid var(--line);
    margin-bottom: 1.25rem;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(15.5rem, 1fr));
    gap: 0.875rem;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .card {
    position: relative;
    padding: 1.125rem 1.25rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
    overflow: hidden;
    transition:
      border-color 180ms ease-out,
      box-shadow 180ms ease-out,
      transform 180ms ease-out;
  }

  .card::after {
    content: '';
    position: absolute;
    width: 4.75rem;
    height: 4.75rem;
    right: -2.25rem;
    bottom: -2.5rem;
    border: 1px solid color-mix(in srgb, var(--accent) 25%, transparent);
    border-radius: 50%;
    pointer-events: none;
  }

  .card__link { position: absolute; inset: 0; border-radius: inherit; }

  .card:hover {
    transform: translateY(-4px);
    border-color: color-mix(in srgb, var(--accent) 48%, var(--line));
    box-shadow:
      0 1px 3px rgb(15 23 42 / 0.05),
      0 20px 40px -28px color-mix(in srgb, var(--accent) 45%, transparent);
  }

  @media (prefers-reduced-motion: reduce) {
    .card:hover {
      transform: none;
    }
  }

  .card__name {
    margin: 0;
    font-size: var(--step-1);
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .card__category {
    margin: 0 0 0.35rem;
    color: var(--accent);
    font-size: var(--step--2);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .card__blurb {
    font-size: var(--step--1);
    color: var(--ink-2);
    flex: 1;
  }

  .card__where {
    font-size: var(--step--2);
    color: var(--ok);
    letter-spacing: 0.04em;
    margin-top: 0.375rem;
  }

  .card__where.is-server {
    color: var(--disclose);
  }

  .card__soon {
    color: var(--muted);
  }

  /* ── Steps ────────────────────────────────────────────────────────── */
  .steps {
    display: grid;
    gap: 1rem;
    margin: 0;
    padding: 0;
    list-style: none;
    grid-template-columns: repeat(auto-fit, minmax(16rem, 1fr));
  }

  .step {
    padding: 1.5rem;
  }

  .step__n {
    font-family: var(--font-mono);
    font-size: var(--step--1);
    color: var(--accent);
  }

  .step__title {
    font-size: var(--step-1);
    margin-block: 0.5rem 0.5rem;
  }

  .step__body {
    font-size: var(--step--1);
    color: var(--ink-2);
  }

  /* No paper, no shadow — a dotted outline where a step would have been. */
  .step--absent {
    padding: 1.5rem;
    border: 1px dashed var(--line-strong);
    border-radius: var(--radius);
    background: none;
    box-shadow: none;
    opacity: 0.62;
  }

  .step--absent .step__n {
    color: var(--muted);
    text-decoration: line-through;
    text-decoration-color: var(--seal);
  }

  .step--absent .step__title {
    color: var(--muted);
  }

  .how__never {
    margin-top: 2rem;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.75rem;
    font-size: var(--step--1);
  }

  .how__strike {
    color: var(--muted);
    text-decoration: line-through;
    text-decoration-color: var(--seal);
    text-decoration-thickness: 1.5px;
    font-family: var(--font-mono);
  }

  .how__seal {
    color: var(--seal);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    font-size: var(--step--2);
    font-weight: 600;
  }

  /* ── Limits ───────────────────────────────────────────────────────── */
  .limits {
    display: grid;
    gap: clamp(2rem, 1.5rem + 3vw, 4rem);
  }

  .limits__intro h2 {
    font-size: var(--step-3);
    margin-bottom: 0.875rem;
  }

  .limits__closing {
    margin-top: 1.25rem;
    font-size: var(--step--1);
    color: var(--muted);
    max-width: 46ch;
  }

  .limits__list {
    margin: 0;
    border-top: 1px solid var(--line);
  }

  .limits__item {
    padding-block: 1rem;
    border-bottom: 1px solid var(--line);
  }

  .limits__item dt {
    font-weight: 600;
    font-size: var(--step-0);
  }

  .limits__item dd {
    margin: 0.25rem 0 0;
    font-size: var(--step--1);
    color: var(--ink-2);
    max-width: 54ch;
  }

  @media (min-width: 62em) {
    .limits {
      grid-template-columns: 0.85fr 1.15fr;
    }
  }

  /* ── Close ────────────────────────────────────────────────────────── */
  .close__line {
    font-size: var(--step-4);
  }

  .close__note {
    margin-top: 1.25rem;
    color: var(--ink-2);
    max-width: var(--measure);
  }

  /* ── Footer ───────────────────────────────────────────────────────── */
  .footer {
    padding-block: clamp(3rem, 2rem + 3vw, 4.5rem) 2rem;
  }

  .footer__cols {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr));
    gap: 2rem;
  }

  .footer__cols h4 {
    font-size: var(--step--2);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--muted);
    font-weight: 500;
    margin: 0 0 0.875rem;
  }

  .footer__cols ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: grid;
    gap: 0.5rem;
  }

  .footer__cols li {
    font-size: var(--step--1);
    color: var(--ink-2);
  }

  .footer__base {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--line);
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem 1.5rem;
    justify-content: space-between;
    align-items: baseline;
  }

  .footer__brand {
    font-weight: 600;
    letter-spacing: -0.02em;
  }

  .footer__colophon {
    font-size: var(--step--2);
    color: var(--muted);
  }
</style>
