---
import BaseLayout from '../layouts/BaseLayout.astro';
import Masthead from '../components/Masthead.astro';
import PdfTool from '../components/PdfTool.astro';
---

<BaseLayout title="Protect a PDF — ProjectPDF" description="Encrypt with AES-256 in your browser. The password never leaves this device — which is the only way a password can stay private.">
  <Masthead />
  <main id="main">
    <PdfTool mode="protect" title="Protect a PDF" description="Encrypt with AES-256 in your browser. The password never leaves this device — which is the only way a password can stay private." />
  </main>
</BaseLayout>
